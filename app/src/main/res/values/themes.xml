<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. Real theming is handled in Compose. -->
    <style name="Theme.PersonaCompanion" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
